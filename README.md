# bip-39-dice
Generate Bitcoin BIP39 mnemonic seed phrases from physical dice rolls. Uses standard base-6 entropy conversion (compatible with Ian Coleman, Cobo Vault). One self-contained HTML file — no network, no dependencies, no build step. Use on an air-gapped device.                                         
